"use client";

// This component is intentionally left blank to remove all banner ads.
export function AdBanner({ page, adIndex = 1, variant = 'square' }: { page: string; adIndex?: number, variant?: 'inline' | 'square' }) {
    return null;
}
