
"use client";

// This component is currently empty and does not render any ads.
// It can be used in the future to integrate a new ad network.

export function AdBanner({ page, adIndex }: { page: string; adIndex?: number }) {
    return null; // Render nothing for now
}
